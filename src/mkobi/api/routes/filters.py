"""Placeholder module for filters routes - CRUD endpoints removed.

The global filter CRUD endpoints (/api/v1/filters) have been removed as they were
orphaned - no frontend code consumed them. Filter-binding functionality for dashboards
remains in dashboards_filters.py and uses FilterRepository directly.
"""
