id: TASK_038_fix_dashboard_detail_type_mismatch

title: Fix DashboardDetail type mismatch with backend response (INT-003)

status: pending

priority: medium

depends_on: []

description: >
  The DashboardDetail interface in api.types.ts is missing fields that the backend
  actually returns (e.g., permission, config with filters/graphs). The frontend
  DashboardView component accesses these fields but they are not in the type.

source_reference:
  - C:\py_dev\mkobi\.ai\audit\validated\final-report.md
  - finding: INT-003

goals:
  - Add missing fields to DashboardDetail interface
  - Align with actual backend DashboardRead model
  - Ensure type safety in DashboardView component

files:
  - path: frontend/src/shared/types/api.types.ts
    targets:
      - type: interface
        name: DashboardDetail
    semantic_anchors:
      - add_fields:
          value: "DashboardDetail"
          description: Add missing fields from backend response

changes:
  - action: modify
    description: >
      Add missing fields to DashboardDetail interface to match the backend
      DashboardRead model.

    code_hint: |
      # BEFORE (lines 153-159):
      export interface DashboardDetail {
        id: string
        name: string
        description: string | null
        config: DashboardConfig
        permission: DashboardPermission
      }

      # AFTER:
      export interface DashboardDetail {
        id: string
        name: string
        description: string | null
        config: DashboardConfig
        permission: DashboardPermission
        created_at: string
        updated_at: string
      }

acceptance_criteria:
      - DashboardDetail includes all fields returned by backend
      - DashboardView component has no type errors
      - Tests pass

risk: low

tests_to_run:
  - "cd frontend && npm test -- --testPathPattern=DashboardView"
