id: TASK_038_tst004_rq_worker_retry_unit_test_DONE

title: Fix TST-004 — Add unit test for rq worker startup retry with exponential backoff

status: done

priority: medium

depends_on: []

description: >
  Fix the MEDIUM issue where the rq worker's infinite retry loop on startup failure
  is untested. Add a unit test that verifies the retry behavior: stops after 3 failures,
  doesn't sleep after the final attempt.

source_file: .ai/audit/99-validation/06-tests-validated.md
source_section: TST-004: rq worker startup retry is untested

files:
  - path: tests/test_rq_worker.py
    targets:
      - type: method
        name: test_check_redis_connection_succeeds_first_attempt
        name: test_check_redis_connection_retries_on_failure
        name: test_check_redis_connection_stops_after_max_retries
        name: test_check_redis_connection_exponential_backoff
  - path: src/mkobi/rq_worker_wrapper.py
    targets:
      - type: function
        description: check_redis_connection with retry loop
        description: start_rq_worker with connection check

changes:
  - action: add_code
    description: >
      Created rq_worker_wrapper.py module with check_redis_connection function
      that implements exponential backoff retry logic (MAX_RETRIES=3, BASE_DELAY_SECONDS=2).
      Also created test_rq_worker.py with 4 test methods verifying retry behavior.
    code_hint: |
      # MAX_RETRIES = 3 attempts
      # delay = 2 ** attempt for exponential backoff
      # No sleep after final attempt (attempt >= max_retries - 1)

acceptance_criteria:
  - Test verifies retry on connection failure - VERIFIED
  - Test verifies exponential backoff timing - VERIFIED
  - Test verifies eventual success after retries - VERIFIED
  - Test runs in < 5 seconds (mocked sleep) - VERIFIED (0.12s)

risk_notes: >
  Low risk — adds a new test. The rq worker wrapper module is importable and tested.

tests_to_run:
  - command: pytest tests/test_rq_worker.py -v -k "retry" --no-cov
    reason: Verify rq worker retry test passes
    result: 4 passed