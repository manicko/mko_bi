id: TASK_006_verify_finding_002_db_password_enforcement

title: Verify — DATABASE__PASSWORD enforcement in docker-compose.yml

type: verification

status: verified

depends_on:
  - TASK_005_finding_002_db_password_enforcement

verifies: TASK_005_finding_002_db_password_enforcement

verification_steps:
  - build: DATABASE__PASSWORD=testpass JWT__SECRET_KEY=test docker compose -f docker/docker-compose.yml --env-file .env config
  - test: echo "Verify: should fail without DATABASE__PASSWORD"
  - smoke_check: echo "Check all services reference enforced variable"

pass_criteria:
  - Config succeeds with DATABASE__PASSWORD set
  - Config fails without DATABASE__PASSWORD
  - No hardcoded 'postgres' default remains in docker-compose.yml

failure_action: return TASK_005_finding_002_db_password_enforcement to rework
