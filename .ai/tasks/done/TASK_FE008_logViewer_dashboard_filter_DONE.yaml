id: TASK_FE008_logViewer_dashboard_filter

title: Fix FE-008 — Implement dashboard filter dropdown in LogViewer

status: pending

priority: medium

depends_on: []

description: |
  The LogViewer dashboard filter dropdown has a TODO comment and no options.
  Implement the filter by loading dashboards via useQuery and getDashboardsAdmin()
  to populate the dropdown with actual dashboard names.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-008: Implement dashboard filter dropdown

files:
  - path: frontend/src/features/admin/ui/LogViewer.tsx
    targets:
      - type: import
        value: getDashboardsAdmin
      - type: component
        name: LogViewer

changes:
  - action: add_code
    description: |
      Add getDashboardsAdmin import and useQuery for dashboard list.
      Replace TODO with mapped MenuItem components.

acceptance_criteria:
  - getDashboardsAdmin imported from adminApi
  - Dashboard list loaded in LogViewer component
  - Dropdown populated with dashboard names and IDs
  - Filter applies selected dashboard_id correctly

risk_notes: |
  Low risk — completes existing feature. getDashboardsAdmin already exists
  in adminApi.ts and returns DashboardAdmin[] with id and name fields.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify build succeeds after implementation
