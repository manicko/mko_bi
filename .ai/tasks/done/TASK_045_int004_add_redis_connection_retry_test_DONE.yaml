id: TASK_045_int004_add_redis_connection_retry_test_DONE

title: Fix INT-004 — Add Redis connection retry test

status: done

priority: low

depends_on: []

description: >
  Fix the LOW issue where Redis connection retry logic is untested.
  Add a test that verifies the retry behavior on connection failure.

source_file: .ai/audit/99-validation/90-integration-validated.md
source_section: INT-004: Redis Connection Retry is Untested

files:
  - path: tests/test_rq_worker.py
    targets:
      - type: method
        name: test_check_redis_connection_succeeds_first_attempt
      - type: method
        name: test_check_redis_connection_retries_on_failure
      - type: method
        name: test_check_redis_connection_stops_after_max_retries
      - type: method
        name: test_check_redis_connection_exponential_backoff
  - path: src/mkobi/rq_worker_wrapper.py
    targets:
      - type: function
        description: check_redis_connection with retry loop

notes: |
  Implementation already exists and was completed in TASK_038.
  
  The task incorrectly referenced INT-004 from integration findings which was about
  error_code type mismatch (str vs ErrorCode), not Redis retry. The Redis connection
  retry logic is implemented in src/mkobi/rq_worker_wrapper.py with:
  - MAX_RETRIES = 3
  - BASE_DELAY_SECONDS = 2  
  - Exponential backoff: delay = 2^attempt seconds
  - All 4 test methods in tests/test_rq_worker.py pass and verify:
    1. Retry on connection failure
    2. Exponential backoff timing
    3. Eventual success after retries
    4. Max retries respected (no sleep after final attempt)

changes:
  - action: none_required
    description: Tests already exist in tests/test_rq_worker.py

acceptance_criteria:
  - Test verifies retry on Redis connection failure - VERIFIED (existing)
  - Test verifies exponential backoff - VERIFIED (existing)
  - Test verifies max retries - VERIFIED (existing)

risk_notes: Already implemented, no risk.

tests_to_run:
  - command: pytest tests/test_rq_worker.py -v --no-cov
    reason: Verify Redis connection retry test passes
    result: 6 passed