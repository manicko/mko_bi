id: TASK_048_int007_add_file_cleanup_test

title: Fix INT-007 — Add temporary file cleanup test after processing failure

status: completed

priority: low

depends_on: []

description: >
  Fix the LOW issue where temporary file cleanup after processing failure is untested.
  Add a test that verifies temp files are deleted even when processing fails.

source_file: .ai/audit/99-validation/90-integration-validated.md
source_section: INT-007: Temp File Cleanup on Failure is Untested

files:
  - path: tests/test_file_cleanup.py
    targets:
      - type: method
        name: test_temp_file_cleaned_up_when_processing_fails

changes:
  - action: add_code
    description: >
      Added TestTempFileCleanupOnProcessingFailure class with test_temp_file_cleaned_up_when_processing_fails
      that creates malformed CSV, triggers processing failure via _process_csv_file_async, and verifies
      temp file cleanup in the exception handler.

acceptance_criteria:
  - Test verifies temp file cleanup on processing failure
  - Test verifies no orphaned temp files

risk_notes: >
  Low risk — adds a new test file.

tests_to_run:
  - command: pytest tests/test_file_cleanup.py -v --no-cov
    reason: Verify file cleanup test passes
