id: TASK_024_verify_finding_014_nginx_security_headers

title: Verify — security headers in nginx.conf

type: verification

status: pending

depends_on:
  - TASK_023_finding_014_nginx_security_headers

verifies: TASK_023_finding_014_nginx_security_headers

verification_steps:
  - build: docker compose -f docker/docker-compose.yml --env-file .env build nginx
  - test: echo "Verify headers in nginx.conf"
  - smoke_check: echo "nginx -t passes with new headers"

pass_criteria:
  - All four security headers present in server block
  - nginx configuration is valid
  - No syntax errors in nginx.conf

failure_action: return TASK_023_finding_014_nginx_security_headers to rework
