id: TASK_FE006_standardize_useAuth_import

title: Fix FE-006 — Standardize useAuth import path in RegisterForm

status: done

priority: low

depends_on: []

description: |
  RegisterForm.tsx imports useAuth from barrel ('../') while LoginForm.tsx
  uses direct path ('../model/useAuth'). Standardize on direct module imports
  for consistency and better traceability.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-006: Standardize useAuth import path

files:
  - path: frontend/src/features/auth/ui/RegisterForm.tsx
    targets:
      - type: import
        value: useAuth

changes:
  - action: replace_code
    description: |
      Replace 'import { useAuth } from '''' with 'import { useAuth } from
      '''../model/useAuth''' to match LoginForm.tsx pattern.

acceptance_criteria:
  - RegisterForm imports useAuth directly from model
  - LoginForm and RegisterForm use same import pattern
  - Build succeeds

risk_notes: |
  Trivial risk — cosmetic import path change.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify build succeeds after import fix
