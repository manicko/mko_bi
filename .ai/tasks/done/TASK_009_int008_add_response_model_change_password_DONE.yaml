id: TASK_009_int008_add_response_model_change_password

title: Fix INT-008 — Add response_model to change-password endpoint

status: pending

priority: low

depends_on: []

description: >
  Add `response_model=SuccessResponse` to POST /auth/change-password endpoint in
  auth.py. This ensures consistent response shape documentation.

source_file: .ai/audit/99-validation/90-integration-validated.md
source_section: INT-008 — Add response_model=SuccessResponse to change_password Endpoint

files:
  - path: src/mkobi/api/routes/auth.py
    targets:
      - type: function
        name: change_password_endpoint
      - type: decorator
        name: router.post
    semantic_anchors:
      - type: decorator
        value: router.post
    changes:
      - action: modify_decorator
        description: >
          Add response_model=SuccessResponse to the decorator and update return
          statement.

acceptance_criteria:
  - Endpoint declares response_model
  - Response uses SuccessResponse model
  - OpenAPI schema updated

risk_notes: >
  Low risk — Pure contract documentation.

tests_to_run:
  - command: uv run mypy src/mkobi/api/routes/auth.py
    reason: Verify type correctness
  - command: uv run pytest tests/test_auth_routes.py -v --no-cov -k "password"
    reason: Verify password change tests pass