id: TASK_018_fe002_remove_unused_generateShortId

title: Fix FE-002 — Remove unused generateShortId function

status: pending

priority: low

depends_on: []

description: >
  Remove the unused generateShortId() function from shortUuid.ts. The function has
  zero call sites in the codebase. Dead code increases maintenance surface.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-002: generateShortId() is defined but never called

files:
  - path: frontend/src/shared/utils/shortUuid.ts
    targets:
      - type: function
        name: generateShortId
    semantic_anchors:
      - type: function_definition
        value: generateShortId
    changes:
      - action: remove_code
        description: >
          Remove the generateShortId function and its JSDoc (lines 20-27).

acceptance_criteria:
  - generateShortId function removed
  - shortUuid function still works
  - TypeScript compiles successfully

risk_notes: >
  No risk — Function is unused.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify TypeScript compilation succeeds
  - command: cd frontend && grep -r "generateShortId" --include="*.ts" --include="*.tsx" | wc -l
    reason: Verify no remaining references