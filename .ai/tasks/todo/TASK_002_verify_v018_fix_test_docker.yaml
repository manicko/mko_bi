id: TASK_002_verify_v018_fix_test_docker

title: "Verify V-018: Test Docker fix unblocks integration tests"

type: verification

status: pending

depends_on:
  - TASK_001_v018_fix_test_docker

verifies: TASK_001_v018_fix_test_docker

verification_steps:
  - build: "docker compose -f docker/docker-compose.test.yml up --build -d"
  - test: "docker compose -f docker/docker-compose.test.yml run test pytest tests/ --tb=short -q"
  - smoke_check: "docker compose -f docker/docker-compose.test.yml run test python -c \"import asyncpg; print('asyncpg OK')\""

pass_criteria:
  - All containers start without errors
  - No "role 'mkobi_app' does not exist" errors in test output
  - Test pass rate significantly improved (target: <173 failures)

failure_action: return TASK_001_v018_fix_test_docker to rework
