---

id: TASK_003_verify_guide

title: "Verify Dashboard Creation Guide compliance"

status: pending

priority: high

depends_on:
  - TASK_002_guide_second_half

type: verification

wave: 3

role: validator

autonomous: true

source_reference: "C:\\py_dev\\mkobi\\.ai\\plans\\PLAN_01.md"

source_section: "Wave 3: Verification — TASK_003"

verifies:
  - TASK_001_guide_first_half
  - TASK_002_guide_second_half

description: >
  Validate the complete docs/11-guides/create-dashboard.md against PLAN_01.md must_haves,
  doc-maintenance-rules.md, DECISION_01.md locked decisions, and terminology standards.
  This is a verification-only task — no content creation or editing.

verification_steps:
  - check: Validate YAML frontmatter contains all required fields (id, domain, tags, related)
  - check: Validate ## Purpose section exists immediately after frontmatter
  - check: Validate ## Prerequisites section contains admin role one-liner
  - check: Validate all lifecycle sections present in chronological order:
      Pre-Creation Planning → Create → Configure Layout → Add Graphs → Add/Bind Filters →
      Set Up Processing Config → Upload Data → Verify Results → Grant Dashboard Access →
      Ongoing Operations → Troubleshooting → Appendix → DB/API Reference → Cross-Links
  - check: Validate layout section is brief conceptual overview — no layouts.definition JSONB
  - check: Validate processing config section links to 03-processing/processing-api.md (no deep coverage)
  - check: Validate appendix has 2–3 annotated JSON processing config examples
  - check: Validate DB/API Reference section present with summary table
  - check: Validate Cross-Links section present at end of file
  - check: Validate all cross-links use ../ relative paths from 11-guides/
  - check: Validate no screenshots — all UI descriptions are textual
  - check: Validate English-only content (no non-English text)
  - check: Validate line count below 800 (soft split threshold)
  - check: Validate terminology consistency:
      "graph" not "chart", "dimension"/"metrics" not "fields",
      "permission" levels view/edit/admin, "filter" types select/multiselect/range/date,
      "processing config" not "data config", "upload" not "import",
      "aggregated data" not "raw data"
  - check: Validate no deferred topics covered:
      no auto-wiring depth, no layout JSONB schema
  - check: Validate all 16 must_haves from PLAN_01.md satisfied

pass_criteria:
  - All verification checks pass
  - Guide is complete, compliant with doc-maintenance-rules.md
  - All cross-links resolve to existing files
  - All must_haves from PLAN_01.md satisfied

failure_action: return TASK_001_guide_first_half and TASK_002_guide_second_half to rework

risk: low

tests_to_run: []
