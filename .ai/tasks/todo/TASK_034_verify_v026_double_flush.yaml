id: TASK_034_verify_v026_double_flush

title: "Verify V-026: Duplicate flush removed"

type: verification

status: pending

depends_on:
  - TASK_033_v026_double_flush

verifies: TASK_033_v026_double_flush

verification_steps:
  - build: "ruff check tests/test_auth.py"
  - test: "pytest tests/test_auth.py -x --tb=short -k 'register_request'"
  - smoke_check: "grep -c 'flush()' tests/test_auth.py | head -5"

pass_criteria:
  - Test passes
  - Only one flush() call in test_register_request_success

failure_action: return TASK_033_v026_double_flush to rework
