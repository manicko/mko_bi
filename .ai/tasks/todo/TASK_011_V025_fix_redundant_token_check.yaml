id: TASK_011_V025
title: Fix redundant token check in axios interceptor
status: pending
priority: low
depends_on: []
wave: 1

description: >
  getTokenWithExpirationCheck() is called twice per request (lines 16 and 19) in
  the axios interceptor. Store result in variable and reuse.

goals:
  - Single call to getTokenWithExpirationCheck per request
  - Result stored in variable and reused
  - No functional change, only performance improvement

files:
  - path: frontend/src/shared/api/axiosInstance.ts
    targets:
      - type: function
        name: request interceptor
    semantic_anchors:
      insert_after:
        type: function_call
        value: getTokenWithExpirationCheck
    changes:
      - action: modify_code
        description: Store the result of getTokenWithExpirationCheck() in a variable. Use the variable for both the token check and the Authorization header assignment. Remove the second call.

acceptance_criteria:
  - getTokenWithExpirationCheck called only once per request
  - Authorization header still set correctly
  - Token expiration check still works
  - Frontend builds without errors

tests_to_run:
  - frontend/src/

risk_level: low
risk_notes: Trivial fix. Store result in variable.

validated_finding: V-025
severity: LOW
