id: TASK_002_FE_toast_config

title: Configure react-hot-toast Toaster with correct options

status: pending

priority: high

depends_on: []

description: >
  Update the Toaster configuration in providers.tsx with position (top-right),
  auto-dismiss durations (3s success, 5s errors), gutter spacing, and manual dismiss support.

goals:
  - Toaster positioned at top-right
  - Success toasts auto-dismiss after ~3000ms
  - Error toasts auto-dismiss after ~5000ms
  - Multiple toasts stack vertically with 8px gutter
  - Manual dismiss available on all toasts

files:
  - path: frontend/src/app/providers.tsx
    targets:
      - type: component
        name: App
    semantic_anchors:
      update_zone:
        type: jsx_element
        value: <Toaster>

changes:
  - action: update_code
    description: Replace <Toaster position="top-right" /> with configured Toaster including toastOptions and gutter

acceptance_criteria:
  - Toaster configured with correct position and durations
  - No visual regressions in the app layout
  - Success toast disappears after ~3s
  - Error toast disappears after ~5s
  - Multiple toasts stack vertically

tests_to_run:
  - cd frontend && npx tsc --noEmit

risk_level: low

wave: 1

plan_source: PLAN_01_toast_config.md
