---
id: TASK_005_admin_migration

title: Migrate admin.py HTTPException sites to AppException

status: pending

priority: medium

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 3 — TASK_03_01 — Migrate admin.py
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_05_admin_migration.yaml

description: >
  Migrate all 20 HTTPException raise sites in admin.py to use AppException with ErrorCode.

goals:
  - Replace all raise HTTPException with raise AppException(code=ErrorCode.X, detail="...")
  - Preserve admin role checks without weakening
  - Add details dict where useful context exists

files:
  - path: src/mkobi/api/routes/admin.py
    targets:
      - type: module
        name: admin
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: replace_code
        description: >
          Replace HTTPException import with AppException import.
          For each of the 20 raise sites: map to appropriate ErrorCode
          (USER_NOT_FOUND, DASHBOARD_NOT_FOUND, PERMISSION_DENIED, EMAIL_ALREADY_EXISTS,
          VALIDATION_ERROR, NOT_FOUND, ACCESS_DENIED). Add details dict where useful
          (e.g., {"user_id": user_id}). Verify admin role checks still function correctly.

acceptance_criteria:
  - [ ] All 20 HTTPException raise sites replaced with AppException
  - [ ] Each raise uses an ErrorCode enum value
  - [ ] Admin role checks still function
  - [ ] No HTTPException import remains
  - [ ] All messages in English

tests_to_run:
  - uv run ruff check src/mkobi/api/routes/admin.py
  - uv run mypy src/mkobi/api/routes/admin.py
  - uv run pytest tests/ -v -k "admin" --no-header 2>&1 | Select-Object -Last 20

risks:
  - Admin permission checks are security-critical — must not be weakened
  - User management operations must maintain audit trail

estimated_effort: medium
risk_level: medium
wave: 3
