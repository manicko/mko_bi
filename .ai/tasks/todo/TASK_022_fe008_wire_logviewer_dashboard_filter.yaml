id: TASK_022_fe008_wire_logviewer_dashboard_filter

title: Fix FE-008 — Wire LogViewer dashboard filter dropdown

status: pending

priority: medium

depends_on: []

description: >
  The LogViewer dashboard dropdown is non-functional (empty). Wire it to
  getDashboardsAdmin() to populate the list.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-008: LogViewer Dashboard filter is non-functional

files:
  - path: frontend/src/features/admin/ui/LogViewer.tsx
    targets:
      - type: component
        name: LogViewer
      - type: query_hook
    semantic_anchors:
      - type: function_call
        value: useQuery
      - type: jsx_comment
        value: TODO
    changes:
      - action: add_code
        description: >
          Add useQuery for getDashboardsAdmin and populate MenuItem list.

acceptance_criteria:
  - Dashboard filter dropdown populated with dashboards
  - UseQuery integrated correctly
  - Lint passes

risk_notes: >
  Low risk — Uses existing getDashboardsAdmin function.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify TypeScript compilation succeeds