id: TASK_FE002_remove_unused_generateShortId

title: Fix FE-002 — Remove unused generateShortId() function

status: pending

priority: low

depends_on: []

description: |
  Remove the unused generateShortId() function from shortUuid.ts. The function
  is defined but never called anywhere in the application. Only shortUuid()
  (which truncates existing IDs) is actively used.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-002: Remove unused generateShortId

files:
  - path: frontend/src/shared/utils/shortUuid.ts
    targets:
      - type: function
        name: generateShortId

changes:
  - action: delete_code
    description: |
      Remove lines 20-27 (the generateShortId function and its JSDoc).
      Keep only shortUuid() function which is actively imported by 3 files.

acceptance_criteria:
  - generateShortId function removed from shortUuid.ts
  - shortUuid function remains intact and working
  - Build succeeds after removal

risk_notes: |
  Trivial risk — pure code removal with no runtime impact.
  The function has zero call sites across the codebase.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify build succeeds after removal
