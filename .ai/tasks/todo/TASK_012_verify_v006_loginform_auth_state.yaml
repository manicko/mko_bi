id: TASK_012_verify_v006_loginform_auth_state

title: "Verify V-006: LoginForm delegates to useAuth hook"

type: verification

status: pending

depends_on:
  - TASK_011_v006_loginform_auth_state

verifies: TASK_011_v006_loginform_auth_state

verification_steps:
  - build: "cd frontend && npm run build"
  - test: "cd frontend && npm run test -- --watchAll=false 2>/dev/null || echo 'no frontend tests'"
  - smoke_check: "cd frontend && npx tsc --noEmit"

pass_criteria:
  - Frontend builds without errors
  - TypeScript compilation passes
  - No direct setToken calls in LoginForm

failure_action: return TASK_011_v006_loginform_auth_state to rework
