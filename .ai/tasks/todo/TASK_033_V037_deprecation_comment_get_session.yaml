id: TASK_033_V037_deprecation_comment_get_session

title: Add deprecation comment to get_session backwards compatibility import in deps.py

status: pending

priority: low

depends_on: []

description: >
  get_session is imported with # noqa: F401 for backwards compatibility, but it's
  unclear who imports it from here. This task adds a deprecation comment with a
  timeline for removal.

finding_ref: V-037
severity: LOW

goals:
  - Add deprecation comment to get_session import
  - Document timeline for removal
  - Clarify implicit coupling

files:
  - path: src/mkobi/api/deps.py
    targets:
      - type: import_statement
        name: get_session with noqa: F401
    semantic_anchors:
      add: deprecation comment above import

changes:
  - action: add_comment
    description: >
      Add a deprecation comment explaining why get_session is imported and
      when it can be removed.
    code_hint: |
      # DEPRECATED: get_session is kept for backwards compatibility.
      # External code may import it from here. Remove in v2.0.
      from mkobi.db.session import get_session  # noqa: F401

acceptance_criteria:
  - Deprecation comment added
  - Timeline for removal documented
  - No functional changes

tests_to_run:
  - pytest tests/ -v

risk_level: low

rollout_notes: >
  Safe. Comment-only change. No dependencies.
