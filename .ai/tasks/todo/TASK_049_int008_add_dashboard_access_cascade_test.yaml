id: TASK_049_int008_add_dashboard_access_cascade_test

title: Fix INT-008 — Add dashboard access cascade delete test

status: pending

priority: low

depends_on: []

description: >
  Fix the LOW issue where dashboard access cascade delete is untested.
  Add a test that verifies access records are deleted when a dashboard is deleted.

source_file: .ai/audit/99-validation/90-integration-validated.md
source_section: INT-008: Dashboard Access Cascade Delete is Untested

files:
  - path: tests/test_dashboard_access.py
    targets:
      - type: method
        name: test_dashboard_access_cascade_delete

changes:
  - action: add_code
    description: >
      Add test_dashboard_access.py with a test that: (1) creates a dashboard,
      (2) grants access to a user, (3) deletes the dashboard, (4) verifies
      the access record is also deleted.

acceptance_criteria:
  - Test verifies cascade delete of access records
  - Test verifies no orphaned access records

risk_notes: >
  Low risk — adds a new test file.

tests_to_run:
  - command: pytest tests/test_dashboard_access.py -v --no-cov
    reason: Verify dashboard access cascade test passes
