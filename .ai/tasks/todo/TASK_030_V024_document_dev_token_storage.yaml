id: TASK_030_V024
title: Document dev-only token storage in sessionStorage
status: pending
priority: low
depends_on: []
wave: 3

description: >
  Development mode uses sessionStorage for tokens. Document this as dev-only
  behavior. Consider memory-only for dev too.

goals:
  - Dev-only token storage behavior documented
  - Clear comment in authToken.ts explaining the behavior
  - No production impact (production uses memory storage)

files:
  - path: frontend/src/features/auth/model/authToken.ts
    targets:
      - type: function
        name: getToken / setToken
    semantic_anchors:
      insert_before:
        type: conditional
        value: sessionStorage check
    changes:
      - action: add_code
        description: Add comments explaining that sessionStorage is used in development for convenience (persists across page refreshes). Production builds use memory-only storage. This is intentional and not a security concern since dev mode is local-only.

acceptance_criteria:
  - authToken.ts has clear comments about dev-only sessionStorage usage
  - No code behavior changes
  - Frontend builds without errors

tests_to_run:
  - frontend/src/

risk_level: low
risk_notes: Documentation only. Dev-only behavior.

validated_finding: V-024
severity: LOW
