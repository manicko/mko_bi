---
id: TASK_012_dp003_test_transaction_wrapper

title: "DP-003: Add transaction wrapper for _store_aggregates in test mode"

status: pending

priority: medium

depends_on:
  - TASK_001_be001_rate_limiter_fix
  - TASK_002_fe006_grant_access_dashboard_id

type: implementation

description: >
  The `_store_aggregates` function in `data_worker.py` lacks a `session.begin()` transaction
  wrapper in the test code path. This can lead to inconsistent database state when tests fail
  mid-operation. Fix: wrap the aggregate storage logic in a `session.begin()` context manager.

source_reference: "C:\\py_dev\\mkobi\\.ai\\audit\\99-validation\\07-data-processing-validated.md"
source_section: "DP-003: Missing transaction wrapper in test mode for _store_aggregates — MEDIUM (mandatory)"

affected_files:
  - path: "src/mkobi/data/processing/data_worker.py"
    targets:
      - type: function
        name: _store_aggregates
    semantic_anchors:
      insert_after:
        type: function_definition
        value: "async def _store_aggregates"
    changes:
      - action: add_code
        description: "Wrap aggregate storage in session.begin() transaction"
        code_hint: |
          async with session.begin():
              # Existing aggregate storage logic
              await session.execute(insert(AggregatedData).values(...))
              await session.flush()

acceptance_criteria:
  - "_store_aggregates uses session.begin() for transaction safety"
  - "Test mode and production mode both work correctly"
  - "ruff + mypy pass"

tests_to_run:
  - command: "cd C:\\py_dev\\mkobi && python -m pytest tests/test_data_transformations.py -v -x"
    description: "Run data transformation tests"

risk: low

estimated_effort: small

tags:
  - medium
  - data-processing
  - transaction
  - test-mode
  - DP-003
