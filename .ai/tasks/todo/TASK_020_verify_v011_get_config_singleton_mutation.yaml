id: TASK_020_verify_v011_get_config_singleton_mutation

title: "Verify V-014: Config singleton no longer mutated in tests"

type: verification

status: pending

depends_on:
  - TASK_019_v011_get_config_singleton_mutation

verifies: TASK_019_v011_get_config_singleton_mutation

verification_steps:
  - build: "ruff check src/mkobi/core/security.py && mypy src/mkobi/core/security.py"
  - test: "pytest tests/test_security.py tests/test_config.py -x --tb=short"
  - smoke_check: "python -c \"from mkobi.core.security import _get_config; print('import OK')\""

pass_criteria:
  - ruff + mypy pass
  - Security and config tests pass
  - No singleton mutation warnings

failure_action: return TASK_019_v011_get_config_singleton_mutation to rework
