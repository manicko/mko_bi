---
id: TASK_010_layouts_migration

title: Migrate layouts.py HTTPException sites to AppException

status: pending

priority: low

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 4 — TASK_04_04 — Migrate layouts.py
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_10_layouts_migration.yaml

description: >
  Migrate all 15 HTTPException raise sites in layouts.py to use AppException with ErrorCode.

goals:
  - Replace all raise HTTPException with raise AppException(code=ErrorCode.X, detail="...")

files:
  - path: src/mkobi/api/routes/layouts.py
    targets:
      - type: module
        name: layouts
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: replace_code
        description: >
          Replace HTTPException import with AppException import.
          For each raise site: map to appropriate ErrorCode (LAYOUT_NOT_FOUND,
          DASHBOARD_NOT_FOUND, PERMISSION_DENIED, VALIDATION_ERROR).
          Add details dict where useful.

acceptance_criteria:
  - [ ] All HTTPException raise sites replaced with AppException
  - [ ] Each raise uses an ErrorCode enum value
  - [ ] No HTTPException import remains
  - [ ] All messages in English

tests_to_run:
  - uv run ruff check src/mkobi/api/routes/layouts.py
  - uv run mypy src/mkobi/api/routes/layouts.py
  - uv run pytest tests/ -v -k "layout" --no-header 2>&1 | Select-Object -Last 20

risks:
  - Layout operations may have nested raise sites

estimated_effort: small
risk_level: low
wave: 4
