---

id: TASK_002_guide_second_half

title: "Write second half of Dashboard Creation Guide (sections 8–15)"

status: pending

priority: high

depends_on:
  - TASK_001_guide_first_half

type: implementation

wave: 2

role: implementor

autonomous: true

source_reference: "C:\\py_dev\\mkobi\\.ai\\plans\\PLAN_01.md"

source_section: "Wave 2: Guide Writing Continuation — TASK_002"

description: >
  Append the second half of guide content to the existing docs/11-guides/create-dashboard.md:
  Add/Bind Filters, Set Up Processing Config, Upload Data, Verify Results, Grant Dashboard Access,
  Ongoing Operations, Troubleshooting, Appendix (3 annotated JSON examples), DB/API Reference,
  and Cross-Links section. This is a documentation-only task continuing the work from TASK_001.

goals:
  - Append all remaining sections to create-dashboard.md
  - Add Appendix with 2–3 annotated JSON processing config examples (simple → complex)
  - Add DB/API Reference section with compact summary table of relevant tables
  - Add Cross-Links section at the end of the file
  - Maintain consistent terminology throughout

files:
  - path: docs/11-guides/create-dashboard.md
    targets:
      - type: file
        name: create-dashboard.md
    semantic_anchors:
      insert_after:
        type: section_boundary
        value: "## Add Graphs"
    changes:
      - action: append_content
        description: Append all second-half sections to the existing file.
      - action: add_content
        description: >
          Write ## Add/Bind Filters section — cover filter types
          (select/multiselect/range/date), binding to dashboard,
          cross-link to ../02-dashboards/dashboards-api.md.
      - action: add_content
        description: >
          Write ## Set Up Processing Config section — CSV parsing settings overview,
          cross-link to ../03-processing/processing-api.md for field-level detail,
          reference appendix for annotated examples. Do NOT cover auto-wiring or
          column type detection behavior in depth.
      - action: add_content
        description: >
          Write ## Upload Data section — describe POST /upload/{dashboard_id},
          file constraints (MIME-type, size), upload mode, task queue lifecycle,
          cross-link to ../03-processing/processing-api.md.
      - action: add_content
        description: >
          Write ## Verify Results section — check processing status, view uploaded data,
          apply filters, cross-link to ../00-overview/data-flow.md.
      - action: add_content
        description: >
          Write ## Grant Dashboard Access section — describe POST/GET/DELETE
          /dashboards/{id}/access, permission levels (view/edit/admin),
          cross-link to ../02-dashboards/dashboards-api.md and ../09-database/schema-access.md.
      - action: add_content
        description: >
          Write ## Ongoing Operations section — cover re-uploading data, modifying
          processing config, managing filters, re-granting access.
      - action: add_content
        description: >
          Write ## Troubleshooting section — cover common issues:
          upload rejected, processing stuck, no data after upload, filter not working,
          access grant not working.
      - action: add_content
        description: >
          Write ## Appendix with 2–3 annotated JSON processing config examples:
          simple (basic CSV with explicit types), medium (with date parsing),
          complex (multiple dimension/metric mappings, custom separators).
      - action: add_content
        description: >
          Write ## DB/API Reference section — compact summary table of relevant tables
          (dashboards, processing_configs, processing_status, dashboard_permissions,
          dashboard_dimension_filters, dashboard_filter_values, aggregated_data)
          with links to schema docs.
      - action: add_content
        description: >
          Write ## Cross-Links section — list all referenced docs with full relative
          paths from 11-guides/.

acceptance_criteria:
  - All second-half sections appended to existing create-dashboard.md
  - Appendix has 2–3 annotated JSON processing config examples (simple, medium, complex)
  - ## DB/API Reference section present with summary table of relevant tables
  - ## Cross-Links section present at end of file
  - All cross-links use ../ relative paths
  - No screenshots — all UI described textually
  - All content in English
  - No deferred topics covered (no auto-wiring depth, no layout JSON schema)
  - File line count below 800 (soft split threshold per doc-maintenance-rules.md)
  - Terminology consistent: "graph" (not "chart"), "dimension"/"metrics" (not "fields"),
    "permission" with levels view/edit/admin, "filter" with types select/multiselect/range/date,
    "processing config" (not "data config"), "upload" (not "import"),
    "aggregated data" (not "raw data")

risk: low

# Inline verification — single file, documentation-only, low risk.
# Implementor checks acceptance_criteria before marking complete.
tests_to_run: []
