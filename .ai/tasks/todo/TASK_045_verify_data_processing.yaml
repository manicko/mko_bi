id: TASK_045_verify_data_processing

title: Verify — Data processing pipeline fixes (DP-001, DP-003, DP-004, DP-008) + tests

type: verification

status: pending

depends_on:
  - TASK_015_add_streaming_size_check
  - TASK_016_fix_mime_type_validation
  - TASK_017_fix_silent_exception_swallowing
  - TASK_018_fix_yoy_inf_values
  - TASK_038_test_mime_type_validation
  - TASK_039_test_streaming_size_limit

verifies:
  - TASK_015_add_streaming_size_check
  - TASK_016_fix_mime_type_validation
  - TASK_017_fix_silent_exception_swallowing
  - TASK_018_fix_yoy_inf_values
  - TASK_038_test_mime_type_validation
  - TASK_039_test_streaming_size_limit

verification_steps:
  - build: docker compose build backend
  - test: pytest tests/ -v -k "upload or file or processing or aggregate or yoy or mime or streaming" --tb=short
  - smoke_check: ruff check src/mkobi/ && mypy src/mkobi/

pass_criteria:
  - Docker build succeeds
  - All data processing tests pass (including new MIME validation and size limit tests)
  - File upload enforces size limit without Content-Length
  - MIME type detected from content, not header
  - No bare except blocks in data_worker.py
  - YoY calculation produces no inf values

failure_action: Return relevant implementation or test task to rework

risk: low
