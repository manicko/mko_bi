id: TASK_034_fe007_add_debounce_to_filter_search

title: Fix FE-007 — Add debounce to filter search input in DashboardFilters

status: pending

priority: low

depends_on: []

description: >
  Fix the LOW issue where filter search input in DashboardFilters triggers
  re-renders on every keystroke. Add debounce to the search input handler.

source_file: .ai/audit/99-validation/02-frontend-validated.md
source_section: FE-007: Add Debounce to Filter Search

files:
  - path: frontend/src/features/dashboards/ui/DashboardFilters.tsx
    targets:
      - type: component
        name: DashboardFilters
    semantic_anchors:
      find:
        type: event_handler
        value: onChange for search input

changes:
  - action: replace_code
    description: >
      Add debounce (300ms) to the search input onChange handler using
      useDebounce from shared/hooks or a custom implementation.

acceptance_criteria:
  - Search input debounced at 300ms
  - No re-render on every keystroke
  - Final search value still applied correctly

risk_notes: >
  Trivial risk — standard debounce pattern.

tests_to_run:
  - command: cd frontend && npm run build
    reason: Verify build succeeds
