---
id: TASK_007_deps_migration

title: Migrate deps.py HTTPException sites to AppException

status: pending

priority: high

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 4 — TASK_04_01 — Migrate deps.py
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_07_deps_migration.yaml

description: >
  Migrate all 16 HTTPException raise sites in deps.py to use AppException with ErrorCode.
  HIGH RISK: deps.py is used by ALL endpoints. FastAPI DI system must continue working.

goals:
  - Replace all raise HTTPException with raise AppException(code=ErrorCode.X, detail="...")
  - FastAPI dependency injection must still work correctly
  - AppException must be caught by registered handler, not FastAPI's default

files:
  - path: src/mkobi/api/deps.py
    targets:
      - type: module
        name: deps
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: replace_code
        description: >
          Replace HTTPException import with AppException import.
          For each of the 16 raise sites in dependency functions: map to appropriate
          ErrorCode (AUTHENTICATION_FAILED, TOKEN_EXPIRED, TOKEN_REVOKED, INVALID_TOKEN,
          PERMISSION_DENIED, INSUFFICIENT_PERMISSIONS, DASHBOARD_NOT_FOUND, USER_NOT_FOUND).
          Preserve detail messages. CRITICAL: verify dependency functions still work
          with FastAPI DI system — AppException must propagate to registered handler.

acceptance_criteria:
  - [ ] All 16 HTTPException raise sites replaced with AppException
  - [ ] Each raise uses an ErrorCode enum value
  - [ ] FastAPI dependency injection still works correctly
  - [ ] No HTTPException import remains
  - [ ] All messages in English

tests_to_run:
  - uv run ruff check src/mkobi/api/deps.py
  - uv run mypy src/mkobi/api/deps.py
  - uv run pytest tests/ -v --no-header 2>&1 | Select-Object -Last 30

risks:
  - deps.py is used by ALL endpoints — breaking it breaks the entire API
  - AppException raised in dependencies must be caught by the registered exception handler
  - Must verify that FastAPI's DI system correctly propagates AppException to the handler

estimated_effort: medium
risk_level: high
wave: 4
