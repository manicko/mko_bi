---
id: TASK_006_dashboards_crud_migration

title: Migrate dashboards_crud.py HTTPException sites to AppException

status: pending

priority: medium

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 3 — TASK_03_02 — Migrate dashboards_crud.py
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_06_dashboards_crud_migration.yaml

description: >
  Migrate all 14 HTTPException raise sites in dashboards_crud.py to use AppException with ErrorCode.

goals:
  - Replace all raise HTTPException with raise AppException(code=ErrorCode.X, detail="...")
  - Preserve dashboard access control behavior unchanged

files:
  - path: src/mkobi/api/routes/dashboards_crud.py
    targets:
      - type: module
        name: dashboards_crud
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: replace_code
        description: >
          Replace HTTPException import with AppException import.
          For each of the 14 raise sites: map to appropriate ErrorCode
          (DASHBOARD_NOT_FOUND, PERMISSION_DENIED, ACCESS_DENIED, VALIDATION_ERROR,
          DUPLICATE_RESOURCE). Add details dict where useful (e.g., {"dashboard_id": id}).

acceptance_criteria:
  - [ ] All 14 HTTPException raise sites replaced with AppException
  - [ ] Each raise uses an ErrorCode enum value
  - [ ] Dashboard CRUD operations unchanged
  - [ ] No HTTPException import remains
  - [ ] All messages in English

tests_to_run:
  - uv run ruff check src/mkobi/api/routes/dashboards_crud.py
  - uv run mypy src/mkobi/api/routes/dashboards_crud.py
  - uv run pytest tests/ -v -k "dashboard" --no-header 2>&1 | Select-Object -Last 20

risks:
  - Dashboard access control is security-critical
  - Nested raise sites need careful attention

estimated_effort: small
risk_level: medium
wave: 3
