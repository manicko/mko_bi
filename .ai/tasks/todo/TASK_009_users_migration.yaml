---
id: TASK_009_users_migration

title: Migrate users.py HTTPException sites to AppException

status: pending

priority: medium

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 4 — TASK_04_03 — Migrate users.py
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_09_users_migration.yaml

description: >
  Migrate all 15 HTTPException raise sites in users.py to use AppException with ErrorCode.

goals:
  - Replace all raise HTTPException with raise AppException(code=ErrorCode.X, detail="...")

files:
  - path: src/mkobi/api/routes/users.py
    targets:
      - type: module
        name: users
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: replace_code
        description: >
          Replace HTTPException import with AppException import.
          For each of the 15 raise sites: map to appropriate ErrorCode
          (USER_NOT_FOUND, EMAIL_ALREADY_EXISTS, VALIDATION_ERROR, PERMISSION_DENIED,
          INVALID_PASSWORD). Add details dict where useful (e.g., {"user_id": user_id}).

acceptance_criteria:
  - [ ] All 15 HTTPException raise sites replaced with AppException
  - [ ] Each raise uses an ErrorCode enum value
  - [ ] No HTTPException import remains
  - [ ] All messages in English

tests_to_run:
  - uv run ruff check src/mkobi/api/routes/users.py
  - uv run mypy src/mkobi/api/routes/users.py
  - uv run pytest tests/ -v -k "user" --no-header 2>&1 | Select-Object -Last 20

risks:
  - User management operations are security-sensitive

estimated_effort: small
risk_level: medium
wave: 4
