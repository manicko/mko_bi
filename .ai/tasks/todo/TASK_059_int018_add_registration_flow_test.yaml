id: TASK_059_int018_add_registration_flow_test

title: Fix INT-018 — Add registration request flow integration test

status: pending

priority: low

depends_on: []

description: >
  Fix the LOW issue where the registration request flow is untested end-to-end.
  Add an integration test for the full registration flow.

source_file: .ai/audit/99-validation/90-integration-validated.md
source_section: INT-018: Registration Flow is Untested End-to-End

files:
  - path: tests/test_registration_flow.py
    targets:
      - type: method
        name: test_registration_request_flow

changes:
  - action: add_code
    description: >
      Add test_registration_flow.py with a test that: (1) submits registration
      request, (2) admin approves request, (3) user logs in with temp password,
      (4) user changes password, (5) user accesses dashboard.

acceptance_criteria:
  - Full registration flow tested end-to-end
  - All steps verified

risk_notes: >
  Low risk — adds a new test file.

tests_to_run:
  - command: pytest tests/test_registration_flow.py -v --no-cov
    reason: Verify registration flow test passes
