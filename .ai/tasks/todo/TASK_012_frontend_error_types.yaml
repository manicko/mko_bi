---
id: TASK_012_frontend_error_types

title: Create ApiError interface and extractApiError utility

status: pending

priority: medium

depends_on:
  - TASK_001_error_code_enum
  - TASK_002_exception_handlers

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: Wave 5 — TASK_05_01 — Create ApiError interface and extractApiError utility
source_file: C:\py_dev\mkobi\.ai\plans\tasks\PLAN_01_12_frontend_error_types.yaml

description: >
  Create frontend types and utilities for handling RFC 7807 error responses.
  This is a foundation task for the frontend error handling system.

goals:
  - ApiError interface matching backend RFC 7807 shape
  - extractApiError() utility with graceful degradation through extraction chain
  - Handles both new RFC 7807 format and legacy FastAPI validation format
  - No any types anywhere

files:
  - path: frontend/src/shared/types/api.types.ts
    targets:
      - type: interface
        name: ApiError
    semantic_anchors:
      insert_after:
        type: export_block
    changes:
      - action: add_code
        description: >
          Add ApiError interface matching backend RFC 7807 shape:
          { type: string; title: string; status: number; detail: string; code: string;
          details?: Record<string, unknown> }

  - path: frontend/src/shared/api/errorHandler.ts
    targets:
      - type: function
        name: extractApiError
    semantic_anchors:
      insert_after:
        type: import_block
    changes:
      - action: add_code
        description: >
          Create extractApiError(error: unknown) returning { code, message, details }.
          Extraction chain: AxiosError → error.response.data (check for code field for
          RFC 7807, or detail field for legacy format) → error.message → "Произошла ошибка".
          For 422 validation errors, parse validation array for field-level messages.
          Ensure proper re-export from frontend/src/shared/api/index.ts.

acceptance_criteria:
  - [ ] ApiError interface matches backend RFC 7807 shape
  - [ ] extractApiError() handles new RFC 7807 format
  - [ ] extractApiError() handles legacy FastAPI validation format
  - [ ] extractApiError() provides generic Russian fallback message
  - [ ] No any types used
  - [ ] Proper TypeScript types throughout

tests_to_run:
  - cd frontend; npm run build
  - cd frontend; npm run test

risks:
  - Must handle both new and legacy error formats during transition
  - AxiosError type narrowing must be correct

estimated_effort: small
risk_level: low
wave: 5
