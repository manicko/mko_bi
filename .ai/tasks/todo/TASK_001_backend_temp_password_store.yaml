id: TASK_001_backend_temp_password_store

title: Create TempPasswordStore — Redis-backed one-time password storage

status: pending

priority: high

depends_on: []

source_reference: C:\py_dev\mkobi\.ai\plans\PLAN_01.md
source_section: TASK_001_backend_temp_password_store

description: >
  Create a new TempPasswordStore class in src/mkobi/core/temp_password_store.py.
  This class provides Redis-backed one-time temporary password storage with TTL.
  Passwords are stored with a TTL and deleted immediately upon retrieval.
  Uses Redis pipeline for atomic GET+DELETE to prevent TOCTOU races.

goals:
  - Create TempPasswordStore class with store() and retrieve() async methods
  - Use Redis pipeline for atomic GET+DELETE on retrieval
  - Fail-open on store errors (log but don't crash)
  - Graceful degradation on retrieve errors (return None)

files:
  - path: src/mkobi/core/temp_password_store.py
    targets:
      - type: class
        name: TempPasswordStore
    semantic_anchors:
      insert_after:
        type: import_statement
        value: "import redis.asyncio as aioredis"
      insert_before:
        type: end_of_file
        value: ""

changes:
  - action: create_file
    description: >
      Create the TempPasswordStore class with store() and retrieve() methods.
      The class accepts an aioredis.Redis client and TTL in seconds.
      store() writes to Redis with key "temp_pwd:{token}" and the configured TTL.
      retrieve() uses a Redis pipeline to atomically GET+DELETE, returning the password or None.
      Both methods catch exceptions and log errors (fail-open pattern).

    code_hint: |
      import logging
      from typing import Final

      import redis.asyncio as aioredis

      logger = logging.getLogger(__name__)

      _KEY_PREFIX: Final[str] = "temp_pwd:"


      class TempPasswordStore:
          """Redis-backed one-time temporary password storage.

          Passwords are stored with a TTL and deleted immediately upon retrieval.
          Uses Redis pipeline for atomic GET+DELETE to prevent TOCTOU races.
          """

          def __init__(self, redis_client: aioredis.Redis, ttl_seconds: int = 86400) -> None:
              self._redis = redis_client
              self._ttl = ttl_seconds

          async def store(self, token: str, password: str) -> None:
              """Store a temporary password under the given token with TTL."""
              key = f"{_KEY_PREFIX}{token}"
              try:
                  await self._redis.set(key, password, ex=self._ttl)
                  logger.info("Temp password stored for token %s... (TTL=%ds)", token[:8], self._ttl)
              except Exception as exc:
                  logger.error("Failed to store temp password in Redis: %s", exc)

          async def retrieve(self, token: str) -> str | None:
              """Retrieve and delete a temporary password. Returns None if not found."""
              key = f"{_KEY_PREFIX}{token}"
              try:
                  async with self._redis.pipeline(transaction=True) as pipe:
                      pipe.get(key)
                      pipe.delete(key)
                      results = await pipe.execute()
                  password = results[0]
                  if password is not None:
                      logger.info("Temp password retrieved for token %s...", token[:8])
                  else:
                      logger.warning("Temp password not found for token %s...", token[:8])
                  return password
              except Exception as exc:
                  logger.error("Failed to retrieve temp password from Redis: %s", exc)
                  return None

acceptance_criteria:
  - Class TempPasswordStore exists in src/mkobi/core/temp_password_store.py
  - Class instantiates with aioredis.Redis client and ttl_seconds parameter
  - store() writes to Redis with "temp_pwd:{token}" key and configured TTL
  - retrieve() atomically GET+DELETE via pipeline, returns password or None
  - store() failures are caught and logged (fail-open, no exception propagation)
  - retrieve() failures return None (graceful degradation)
  - All public methods have type hints
  - Module uses logger = logging.getLogger(__name__)

tests_to_run:
  - pytest tests/core/test_temp_password_store.py -v

risk_level: low
