id: TASK_014_verify_v009_remove_sidebar_dead_code

title: "Verify V-009: Sidebar dead code removed, build succeeds"

type: verification

status: pending

depends_on:
  - TASK_013_v009_remove_sidebar_dead_code

verifies: TASK_013_v009_remove_sidebar_dead_code

verification_steps:
  - build: "cd frontend && npm run build"
  - test: "cd frontend && npx tsc --noEmit"
  - smoke_check: "! test -f frontend/src/shared/components/Layout/Sidebar.tsx && echo 'Sidebar removed' || echo 'Sidebar still exists'"

pass_criteria:
  - Frontend builds without errors
  - TypeScript compilation passes
  - Sidebar.tsx file does not exist
  - No grep matches for "Sidebar" in import/export statements

failure_action: return TASK_013_v009_remove_sidebar_dead_code to rework
