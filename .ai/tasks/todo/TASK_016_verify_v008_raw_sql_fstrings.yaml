id: TASK_016_verify_v008_raw_sql_fstrings

title: "Verify V-008: No f-string SQL, lint passes"

type: verification

status: pending

depends_on:
  - TASK_015_v008_raw_sql_fstrings

verifies: TASK_015_v008_raw_sql_fstrings

verification_steps:
  - build: "ruff check src/mkobi/db/starter.py && mypy src/mkobi/db/starter.py"
  - test: "pytest tests/ -x --tb=short -k 'starter or database'"
  - smoke_check: "python -c \"from mkobi.db.starter import DatabaseStarter; print('import OK')\""

pass_criteria:
  - ruff passes
  - mypy passes
  - No f-string SQL patterns in starter.py

failure_action: return TASK_015_v008_raw_sql_fstrings to rework
