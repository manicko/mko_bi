---
id: TASK_06_02
name: Create error format security documentation
wave: 6
depends_on:
  - TASK_01_01
  - TASK_01_02
files_modified:
  - docs/08-security/error-format.md
autonomous: true
description: |
  Create new documentation file describing the RFC 7807 error format for security context.

  Steps:
  1. Create docs/08-security/error-format.md with:
     - Overview of RFC 7807 Problem Details standard
     - Extended fields (code, details) and their purpose
     - Complete ErrorCode enum reference table (code → HTTP status → description)
     - Example JSON responses for common scenarios:
       - 401 authentication failure
       - 403 permission denied
       - 404 resource not found
       - 422 validation error
       - 500 internal error (note: no internal details exposed)
     - Security considerations:
       - Error messages must not leak sensitive data
       - detail field is for dev consumption, not user display
       - code field is for programmatic handling
       - Global handler catches all unhandled exceptions without leaking internals
     - Frontend integration notes (extraction chain, display strategy)
     - migration notes: HTTPException → AppException

  2. Link from docs/08-security/security-overview.md if it exists
  3. All text in English
acceptance_criteria:
  - [ ] docs/08-security/error-format.md created
  - [ ] Complete ErrorCode reference table included
  - [ ] Example JSON responses provided
  - [ ] Security considerations documented
  - [ ] All text in English
must_haves:
  - RFC 7807 format fully documented
  - Security implications of error responses covered
  - ErrorCode enum reference table complete
validation:
  - Manual review for completeness and accuracy
risks:
  - None — documentation-only change
estimated_effort: small
risk_level: minimal
