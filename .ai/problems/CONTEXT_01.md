Я подготовлю файл docker-compose.test.yml как полностью самостоятельную конфигурацию (без merge с основным) или добавлю в стор-фай тестов переменные, которые обеспечат изоляцию.

Standalone test compose (рекомендую):
Создать docker-compose.test.yml как полностью независимый compose-файл с собственным `` контейнером on:8001, :5433, собственными сетями. Запускать командой docker compose -f docker-compose.test.yml up -d` — параллельно с dev.