# =============================================================================
# Docker Compose Configuration for Testing (STANDALONE)
# Usage: docker compose -f docker/docker-compose.test.yml up -d
#        docker compose -f docker/docker-compose.test.yml exec test-app uv run pytest tests/ -v
#
# SECURITY NOTE (INF-011): These ports are intentionally exposed to the host for
# development workflow convenience. They enable:
#   - Running pytest directly from host terminal (native execution)
#   - Database GUI tools for debugging test database state
#   - Parallel dev+test environment execution (shifted ports avoid conflicts)
#
# Risk Assessment:
#   - LOW severity: Test database contains NO production data
#   - Uses default passwords: test_password (postgres), test_app_password (mkobi_app)
#   - Bind address: 0.0.0.0 (Docker default, localhost-only on most systems)
#   - No CI/CD pipelines found that depend on these ports
#
# Alternatives considered but NOT recommended for removal:
#   - Bind to 127.0.0.1: Adds complexity without significant security gain
#   - Network-only access: Would break documented host-side pytest workflow
#   - Override file: Adds developer friction for common use case
#
# See TASK_055_assess_test_ports_removal_impact for full assessment.
# =============================================================================

services:
  test-db:
    image: postgres:16.3-bookworm
    container_name: test-db
    environment:
      POSTGRES_DB: bidb_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DATABASE__PASSWORD:-test_password}
      MKOBI_APP_PASSWORD: ${MKOBI_APP_PASSWORD:-test_app_password}
    volumes:
      - test_postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d:ro
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
    networks:
      - test_network

  test-redis:
    image: redis:7.4-alpine
    container_name: test-redis
    volumes:
      - test_redis_data:/data
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
        reservations:
          memory: 128M
    networks:
      - test_network

  test-migrate:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: test
    container_name: test-migrate
    command: ["alembic", "upgrade", "head"]
    environment:
      ENV: test
      HOME: /tmp
      UV_CACHE_DIR: /tmp/.cache/uv
      DATABASE__HOST: test-db
      DATABASE__PORT: 5432
      DATABASE__USER: postgres
      DATABASE__PASSWORD: ${DATABASE__PASSWORD:-test_password}
      DATABASE__DBNAME: bidb_test
      DATABASE__TEST_DBNAME: bidb_test
      DATABASE__ADMIN_USER: postgres
      DATABASE__ADMIN_PASSWORD: ${DATABASE__PASSWORD:-test_password}
      REDIS__HOST: test-redis
      REDIS__PORT: 6379
      JWT__SECRET_KEY: ${JWT__SECRET_KEY:-test_jwt_secret_key_for_integration_tests_32_chars}
      JWT__ALGORITHM: HS256
      ADMIN_USERNAME: ${ADMIN_USERNAME:-test-admin@example.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-test-password-for-integration-tests}
    depends_on:
      test-db:
        condition: service_healthy
    restart: "no"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    networks:
      - test_network

  test-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: test
    container_name: test-app
    environment:
      ENV: test
      HOME: /tmp
      UV_CACHE_DIR: /tmp/.cache/uv
      DATABASE__HOST: test-db
      DATABASE__PORT: 5432
      DATABASE__USER: mkobi_app
      DATABASE__PASSWORD: ${MKOBI_APP_PASSWORD:-test_app_password}
      DATABASE__DBNAME: bidb_test
      DATABASE__TEST_DBNAME: bidb_test
      DATABASE__ADMIN_USER: postgres
      DATABASE__ADMIN_PASSWORD: ${DATABASE__PASSWORD:-test_password}
      REDIS__HOST: test-redis
      REDIS__PORT: 6379
      JWT__SECRET_KEY: ${JWT__SECRET_KEY:-test_jwt_secret_key_for_integration_tests_32_chars}
      JWT__ALGORITHM: HS256
      ADMIN_USERNAME: ${ADMIN_USERNAME:-test-admin@example.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-test-password-for-integration-tests}
      LOGGING__LEVEL: WARNING
      RECREATE_TEST_DB: "true"
    ports:
      - "8001:8000"
    depends_on:
      test-migrate:
        condition: service_completed_successfully
      test-db:
        condition: service_healthy
    healthcheck:
      disable: true
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
    networks:
      - test_network
    command: ["tail", "-f", "/dev/null"]

volumes:
  test_postgres_data:
  test_redis_data:

networks:
  test_network:
    driver: bridge

