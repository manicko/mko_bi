## окружение
windows
## backend
- test:`uv run pytest <path>`
- ruff:`uv run ruff check <path>`
- mypy:`uv run mypy <path>`
- alembic: `uv run alembic ...`
- deps-add: `uv add <package>`
- deps-dev:`uv add --dev <package>`
- psql :`set "PGPASSWORD=1234" & psql -h localhost -p 5432 -U postgres -d bidb`

IMPORTANT: 
ALWAYS RUN TO UPDATE PROJECT INFO `C:\py_dev\mkobi\.ai\builders\build.bat`
BEFORE STARTING:
- audit
- implement tasks
- plan
- research

