TASK: Привести код к нулевым ошибкам ruff и mypy

FILE: ВСЕ файлы проекта

GOAL: Устранить все ошибки ruff и mypy для повышения качества кода

IMPLEMENT:

1. Исправить критические ошибки ruff:
   - B008: Depends() в аргументах по умолчанию (28 мест)
   - B904: raise без from (4 места)
   - B905: zip() без strict= (8 мест)
2. Исправить ошибки mypy:
   - Неопределенные имена в моделях
   - Отсутствующие аргументы типов
   - Несовместимые типы
3. Удалить dead code:
   - Неиспользуемые импорты (F401)
   - Неиспользуемые переменные (F841)
   - Пустые файлы и заглушки
4. Добавить аннотации типов:
   - dict → dict[str, Any]
   - list → list[Type]
   - Optional где нужно
5. Исправить импорты:
   - Разрешить циклические через TYPE_CHECKING
   - Использовать from __future__ import annotations

CONSTRAINTS:

- ruff check . должен проходить без ошибок
- mypy должен проходить без ошибок
- Сохранить функциональность
- Добавить аннотации ко всем публичным функциям
- Удалить только действительно неиспользуемый код

DONE:

- ruff check .: 0 ошибок
- mypy: 0 ошибок
- Код типизирован
- Нет dead code
- Улучшена читаемость
- Сохранена функциональность
