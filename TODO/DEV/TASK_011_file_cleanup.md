TASK: Гарантированное удаление временных файлов

FILE: src/mko_bi/services/data_service.py

GOAL: Обеспечить удаление временных файлов даже при ошибках или KeyboardInterrupt

IMPLEMENT:

func: улучшение finally блока для удаления файлов

LOGIC:

1. Проверить текущий finally блок (строки 751-757):
   - Убедиться что file_path определен до try блока
   - Добавить проверку существования файла перед удалением

2. Улучшить обработку ошибок при удалении:
```
finally:
    # Гарантированное удаление файла
    if file_path and file_path.exists():
        try:
            file_path.unlink()
            logger.info("Временный файл удален: %s", file_path)
        except Exception as e:
            logger.error("Ошибка при удалении файла %s: %s", file_path, e)
```

3. Обработка KeyboardInterrupt (опционально):
   - Добавить обработчик сигнала если требуется
   - Или использовать atexit для очистки при завершении

4. Рассмотреть использование временной директории:
   - Вместо сохранения в data/tmp_uploads/ можно использовать tempfile.TemporaryDirectory()

CONSTRAINTS:

- Независимая задача
- Не использовать overengineering (сложные механизмы очистки)
- Просто гарантировать удаление файла в finally
- Логировать ошибки удаления, но не прерывать выполнение

DONE:

- Файл гарантированно удаляется в finally блоке
- Ошибки при удалении логируются, но не прерывают работу
- Временные файлы не накапливаются на диске
- `uv run pytest tests/test_data_service.py` проходит

TEST:

# Создать тестовый сценарий:
# 1. Загрузить файл
# 2. Проверить что временный файл удален после обработки
uv run pytest tests/test_data_service.py -v
