TASK: Улучшить обработку ошибок и исключений

FILE: src/mko_bi/utils/exceptions.py, src/mko_bi/api/routes/, src/mko_bi/services/

GOAL: Внедрить единый подход к обработке ошибок с корректными HTTP статус-кодами и сохранением стека вызовов

IMPLEMENT:

func: HTTPException с правильными статус-кодами
func: CustomException - базовое исключение
func: error_handler - middleware для обработки

LOGIC:

1. В utils/exceptions.py:
   - CustomException (базовое)
   - NotFoundException (404)
   - ValidationException (400)
   - PermissionException (403)
   - ServiceException (500)
2. В middleware:
   - Перехватывать исключения
   - Логировать с полным стеком
   - Возвращать корректные статус-коды
3. В роутах и сервисах:
   - Использовать raise from для сохранения стека
   - Логировать ошибки перед пробросом
   - Не использовать пустые except
   - Дифференцировать 4xx и 5xx
4. HTTP статус-коды:
   - 400: Ошибка валидации
   - 401: Не авторизован
   - 403: Нет прав
   - 404: Не найдено
   - 500: Внутренняя ошибка сервера

CONSTRAINTS:

- Все raise использовать from e
- logger.exception() для полного стека
- Пустые except запрещены
- Четкое разделение 4xx/5xx
- Пользователь не видит деталей 500 ошибок
- Все исключения логируются

DONE:

- Единый формат ошибок
- Корректные статус-коды
- Полный стек в логах
- Нет потери контекста
- Пользовательские сообщения

