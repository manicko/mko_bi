TASK: Реализовать расчет долей и процентов в агрегациях

FILE: src/mko_bi/data/processing/transformations.py

GOAL: Добавить вычисление процентных долей от общего итога для агрегированных данных

IMPLEMENT:

func: calculate_shares(df, groupby_columns, metric_columns)
func: calculate_percentages(df, total_column, share_columns)

LOGIC:

1. В transformations.py:
   - Функция calculate_shares:
     * Вычисляет общий итог по группам
     * Рассчитывает долю каждой строки от итога
     * Возвращает DataFrame с колонками share_* (0-1 или 0-100%)
   - Функция calculate_percentages:
     * Рассчитывает процентное изменение между периодами
     * (текущий - предыдущий) / предыдущий * 100
   - Обработка edge cases:
     * Деление на ноль → 0 или NULL
     * Отрицательные значения
     * Округление до 2 знаков
2. Интеграция в pipeline:
   - Вызов после groupby агрегаций
   - Сохранение долей в JSONB metrics
   - Метаданные о типе агрегации

CONSTRAINTS:

- Использовать Polars для эффективности
- Поддержка нескольких метрик
- Корректное округление
- Сохранение в формате JSONB
- Производительность на больших данных

DONE:

- Доли рассчитываются корректно
- Данные сохраняются в aggregated_data.metrics
- Графики типа pie используют доли
- Таблицы отображают проценты
- Тесты покрывают все сценарии
