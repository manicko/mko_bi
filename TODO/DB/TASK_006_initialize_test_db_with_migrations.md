TASK: Инициализировать тестовую базу данных с применением миграций

FILE: src/mko_bi/tests/conftest.py

GOAL: Добавить pytest фикстуру, которая автоматически применяет Alembic миграции к тестовой базе данных bidb_test перед запуском тестов, чтобы гарантировать, что тесты работают против правильной схемы.

IMPLEMENT:
- Добавить фикстуру с scope="session" и autouse=True, которая запускает `alembic upgrade head` для тестовой базы данных
- Использовать переменную окружения DB_NAME для указания тестовой базы данных (или настроить DSN через переменные окружения)
- Убедиться, что фикстура выполняется один раз per тестовая сессия
- Обработать ошибки: если миграции не применяются, прервать выполнение тестов

LOGIC:
- Фикстура должна подключиться к тестовой базе данных (уже настроенной в conftest.py через переменные окружения)
- Выполнить команду alembic для применения всех миграций
- Если команда возвращает не нулевой код выхода, фикстура должна вызвать исключение, прерывая тесты

CONSTRAINTS:
- Фикстура не должна ломать существующие тесты
- Должна работать с текущей настройкой тестовой базы данных в conftest.py
- Не должна требовать изменений в других файлах, кроме conftest.py

DONE:
- Тестовая база данных bidb_test имеет примененные все миграции перед запуском тестов
- Фикстура выполняется автоматически для всей тестовой сессии
- Тесты начинают работу против актуальной схемы базы данных
- При ошибке применения миграций тесты не запускаются