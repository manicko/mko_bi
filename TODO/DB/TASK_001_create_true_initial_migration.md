TASK: Создать истинную начальную миграцию для воспроизводимой базы данных

FILE: src/mko_bi/alembic/versions/true_initial_migration.py

GOAL: Создать миграцию, которая создает все таблицы с нуля согласно SPEC.md, обеспечивая возможность воспроизведения базы данных с пустого состояния.

IMPLEMENT:
- Создать новую миграцию, которая создает все таблицы (users, layouts, dashboards, graphs, filters, dashboard_access, dashboard_filters, processing_configs, processing_logs, aggregated_data) с правильными типами согласно SPEC.md
- Убедиться, что миграция работает на полностью пустой базе данных
- Обновить цепочку миграций, чтобы зависеть от этой новой начальной миграции
- Переименовать текущую e86f3c8f7324_initial_migration.py в что-то вроде e86f3c8f7324_schema_adjustments.py и обновить её зависимости

LOGIC:
- Миграция должна создавать таблицы с правильными типами: BIGSERIAL для aggregated_data.id, JSONB для layouts.definition и т.д.
- Все ограничения, индексы и типы должны соответствовать SPEC.md
- После создания этой миграции, команда `alembic upgrade head` должна работать на пустой БД

CONSTRAINTS:
- Должна быть совместима с текущими моделями ORM
- Не должна ломать существующие установки при применении после текущих миграций
- Должна использовать правильные типы PostgreSQL (UUID, JSONB, ENUM где specified)

DONE:
- Таблица пользователей создается с правильными типами и ограничениями
- Таблица layouts создается с JSONB типом для definition
- Таблица aggregated_data создается с BIGSERIAL для id
- Можно выполнить `alembic upgrade head` на пустой базе данных и получить рабочую схему
- Все существующие тесты продолжают проходить после применения всех миграций