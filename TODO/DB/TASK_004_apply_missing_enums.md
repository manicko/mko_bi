TASK: Применить недостающие типы ENUM в базе данных

FILE: src/mko_bi/alembic/versions/apply_missing_enums.py

GOAL: Создать недостающие типы ENUM (graph_type, filter_type, processing_status) и изменить соответствующие столбцы для использования этих типов вместо CHECK ограничений, обеспечивая согласованность между ORM моделями и базой данных.

IMPLEMENT:
- Создать миграцию, которая:
  1. Создает типы ENUM: graph_type, filter_type, processing_status
  2. Изменяет столбцы type в таблицах graphs и filters, а также столбец status в таблице processing_logs для использования соответствующих типов ENUM с использованием USING клаузов
- Убедиться, что миграция применяется чисто на пустой базе данных (после всех предыдущих миграций)
- Добавить зависимость от предыдущих миграций в файл миграции

LOGIC:
- Миграция должна использовать CREATE TYPE для создания новых типов ENUM
- Миграция должна использовать ALTER TABLE для изменения типа столбцов с varchar на соответствующий ENOM тип
- После применения миграции, таблицы graphs, filters и processing_logs должны иметь столбцы типа ENUM вместо varchar с CHECK ограничением
- ORM модели уже используют эти типы ENUM, поэтому теперь будет полное согласие

CONSTRAINTS:
- Миграция должна быть совместима с текущими моделями ORM (которые уже используют эти типы ENUM)
- Миграция не должна ломать существующие данные при применении (данные должны соответствовать новым типам)
- Миграция должна быть откатной (предусмотреть downgrade)

DONE:
- Типы ENUM graph_type, filter_type, processing_status созданы в базе данных
- Столбцы graphs.type, filters.type и processing_logs.status используют соответствующие типы ENUM
- Миграция применяется чисто на пустой базе данных после предыдущих миграций
- Миграция имеет рабочий downgrade
- Все существующие тесты продолжают проходить после применения миграции