TASK: Усилить безопасность: rate limiting и валидация данных

FILE: src/mko_bi/app.py, src/mko_bi/core/security.py, src/mko_bi/api/routes/upload.py

GOAL: Добавить rate limiting для всех эндпоинтов и усилить валидацию входных данных

IMPLEMENT:

func: setup_rate_limiter()
func: validate_field_name()
func: validate_csv_content()

LOGIC:

1. Rate limiting:
   - Использовать slowapi или аналог
   - Лимиты: 100 запросов/минуту для обычных пользователей
   - 10 запросов/минуту для загрузки файлов
   - Разные лимиты для разных ролей
   - Хранилище: Redis или in-memory
2. Валидация полей:
   - Белый список для имен колонок
   - Проверка на path traversal
   - Ограничение длины строк
   - Проверка типов
3. Валидация CSV:
   - Проверка структуры
   - Ограничение размера ячеек
   - Защита от формул и скриптов
   - Использовать safe режим Polars
4. CORS:
   - Ограничить конкретными доменами
   - Не использовать allow_any_origin

CONSTRAINTS:

- Rate limiting для всех мутационных эндпоинтов
- Валидация всех входных данных
- Защита от SQL/Polars инъекций
- Защита от path traversal
- Корректные сообщения об ошибках
- Не блокировать легитимных пользователей

DONE:

- Rate limiting работает
- Валидация всех входных данных
- Защита от инъекций
- CORS ограничен
- Нет path traversal уязвимостей
- Тесты на безопасность
