TASK: Добавить тесты точности агрегаций (SUM, COUNT, AVG) для проверки математической корректности

FILE: tests/test_data_processing.py

GOAL: Обеспечить, что функции агрегации правильно вычисляют SUM, COUNT, AVG и другие статистические функции

IMPLEMENT:
- Тесты на точность SUM с положительными, отрицательными и нулевыми значениями
- Тесты на точность COUNT с уникальными и дублирующимися значениями
- Тесты на точность AVG с различными наборами данных
- Тесты на группировку по одному и нескольким измерениям
- Тесты на обработку null значений в агрегациях (должны игнорироваться или обрабатываться согласно бизнес-правилам)
- Тесты на большие наборы данных для проверки производительности и точности при переполнении

LOGIC:
- Для каждого типа агрегации создать набор тестовых данных с известным ожидаемым результатом
- Использовать реальную БД сессию для хранения тестовых данных
- Вызывать функцию агрегации с тестовыми данными
- Сравнивать фактический результат с ожидаемым с допустимой погрешностью (для чисел с плавающей точкой)
- Тестировать edge случаи: пустые наборы, только null значения, большие числа

CONSTRAINTS:
- Использовать только реальную БД, не мокировать функции агрегации
- Тесты должны быть изолированными и очищать БД после каждого теста
- Использовать реалистичные данные, аналогичные тем, что обрабатывает система
- Тесты должны проверять фактическую математическую точность, а не только что функция была вызвана

DONE:
- Тесты покрывают все основные агрегационные функции с проверкой точности
- Тесты используют реальную БД для проверки фактических вычислений
- Тесты ловят ошибки в логике агрегации (например, умножение вместо сложения)
- Тесты устойчивы к рефакторингу и дают уверенность в корректности бизнес-логики