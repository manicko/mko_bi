TASK: Вынести бизнес-логику из API роутов в сервисы

FILE: src/mko_bi/api/routes/dashboards.py, src/mko_bi/api/routes/users.py, src/mko_bi/api/routes/auth.py

GOAL: Привести в соответствие с Clean Architecture - роуты только координируют, вся логика в сервисах

IMPLEMENT:

func: create_dashboard() - перенести в dashboard_service
func: update_user() - перенести в user_service
func: login() - упростить, оставить только аутентификацию

LOGIC:

1. Анализ текущих роутов:
   - dashboards.py: создание, обновление, удаление дашбордов
   - users.py: CRUD пользователей, обновление профиля
   - auth.py: логин, rate limiting, токены
2. Перенос логики:
   - Создание дашборда → dashboard_service.create_dashboard()
   - Обновление пользователя → user_service.update_user()
   - Назначение прав → dashboard_service.update_access()
3. Роуты должны:
   - Валидировать входные данные (Pydantic)
   - Вызывать сервисы
   - Обрабатывать исключения
   - Возвращать ответы
4. Сервисы должны:
   - Содержать всю бизнес-логику
   - Работать с репозиториями
   - Обрабатывать ошибки
   - Возвращать доменные объекты

CONSTRAINTS:

- Роуты не содержат бизнес-логики
- Нет прямого использования репозиториев в роутах
- Сервисы инжектируются через Depends
- Четкое разделение ответственности
- Транзакции в сервисах

DONE:

- Бизнес-логика только в сервисах
- Роуты только координируют
- Чистая архитектура
- Улучшена тестируемость
- Нет дублирования
